import { StyleSheet, Text, View } from 'react-native';

const QRCode: React.FC = () => {
  return (
    <View>
      <Text>QRCode画面です</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default QRCode;
